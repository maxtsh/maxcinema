import React from "react";

export const metadata = {
  title: "Movies",
  description: "MaxTsh Cinema Application",
  icons: "/favicon.ico",
};

const Movie = async () => {
  return (
    <div>
      <h1>Movie</h1>
    </div>
  );
};

export default Movie;
